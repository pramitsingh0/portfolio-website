# Navigation Morph Implementation Plan

**Goal:** Make the glass-first / width-first sequence visible and interruptible.
**Architecture:** Retain Nav.tsx's hook and CSS endpoints; freeze measured geometry independently of React's data-state. Use one GSAP timeline with explicit phase labels.
**Tech Stack:** React, TypeScript, GSAP, Vite.

- [x] Inspect existing navigation, state thresholds, material CSS and working tree.
- [x] Specify timing, alternative approaches, responsive behavior and reduced motion.
- [x] Update useNavMorph in src/components/Nav.tsx: measure target after clearing width/top/maxWidth, immediately restore current geometry with maxWidth none, then sequence material and geometry. Initialize the first render without an entrance morph. Use scaleY .9 for material formation, never scale labels.
- [x] On resize kill the timeline, clear all geometry overrides, measure CSS target and pin it again; settle material to the current state. Remove listener on cleanup.
- [x] Run pnpm build and pnpm lint.
- [x] Inspect browser frames for both directions, interruptions, resize and narrow viewport. Check reduced-motion behavior.

Validation results: pnpm build and pnpm lint passed. Browser samples showed glass opacity rising while width stayed 936px; expansion moved 759px to 936px with material opacity 1 through the hold. Mobile compact state measured 308px; rapid reversal and viewport reset settled at top state, width 936px, opacity 0. Reduced-motion branch reviewed in source (not OS-emulated).
